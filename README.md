# mcp poc

## セットアップ・動作確認手順

※現時点ではmacOS限定

- `npm i`
- `npm run setup`
  - MCPサーバーのトランスパイル
  - MCPサーバー追加済み設定ファイル `.amazonq/agents/default.json` 生成
- Amazon Q VSCode Externsion -> Configure MCP Servers -> Refresh MCP Servers
- チャットで `.amazonq/prompts/poc.md` をメンションして実行
